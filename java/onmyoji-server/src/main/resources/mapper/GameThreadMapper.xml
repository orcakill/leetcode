<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.GameThreadMapper">
  <select id="getById" parameterType="String" resultType="com.example.demo.model.entity.ArticlePO">
    SELECT * FROM game_thread where id=#{id}
  </select>
</mapper>